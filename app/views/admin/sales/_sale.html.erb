<tr>
  <td><%= sale.name %></td>
  <td><%= sale.starts_on %></td>
  <td><%= sale.ends_on %></td>
  <td>
    <% if sale.isActive? %>
      <span class="label label-success"> Active </span>
    <% elsif sale.isUpcoming? %>
      <span class="label label-warning"> Upcoming </span>
    <% else %>
      <span class="label label-danger"> Finished </span>
    <% end %>
  </td>
  <td><%= sale.percent_off %></td>
</tr>